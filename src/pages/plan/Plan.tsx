import React, { ReactElement } from 'react'

interface Props {

}

export default function Plan({ }: Props): ReactElement {
    return (
        <div>
            <div className="mb-20 text-red-800">
                list of plans
            </div>
        </div>
    )
}
