import React, { ReactElement } from 'react'

interface Props {

}

export default function LoginPage({ }: Props): ReactElement {
    return (
        <div>
            this is login page
        </div>
    )
}
